version: '3.8'

services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "10000:10000"
    env_file: .env
    depends_on:
      - db
    restart: always
    volumes:
      - ./backend/uploads:/usr/src/app/uploads

  db:
    image: postgres:15
    restart: always
    volumes:
      - db_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: mvp_database_g0ic

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file: ./frontend/.env.local
    environment:
      - NEXT_PUBLIC_API_BASE_URL=http://api.polimee.com:10000
    restart: always
    depends_on:
      - api

  admin-frontend:
    build:
      context: ./admin-frontend
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    env_file: ./admin-frontend/.env.local
    environment:
      - NEXT_PUBLIC_API_BASE_URL=http://api.polimee.com:10000
    restart: always
    depends_on:
      - api

volumes:
  db_data:
    driver: local
